% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logger_lumberjack.R
\name{logrittr_logger}
\alias{logrittr_logger}
\title{logrittr logger for lumberjack}
\description{
An R6 logger that plugs into the lumberjack \verb{\%L>\%} pipe and renders the
same console output as \verb{\%>=\%}: row/column counts, signed deltas,
added/dropped column names, and approximate step timing.

Requires the \code{R6} and \code{lumberjack} packages (both in \code{Suggests}).
If either is missing, the class is unavailable but the rest of logrittr
works normally.
}
\section{Timing}{

lumberjack calls \verb{$add()} after each step without providing a start time.
Elapsed time is measured as the interval between two consecutive \verb{$add()}
calls. The first step always shows \verb{NA ms}.
}

\examples{
\dontrun{
library(lumberjack)
library(dplyr)

iris \%L>\%
  start_log(log = logrittr_logger$new()) \%L>\%
  as_tibble() \%L>\%
  filter(Sepal.Length < 5) \%L>\%
  mutate(rn = row_number()) \%L>\%
  group_by(Species) \%L>\%
  summarise(n = n_distinct(rn)) \%L>\%
  dump_log(stop = TRUE)
  
  
logfile <- tempfile(fileext="r.log.csv")

iris \%L>\%
  start_log(log = logrittr_logger$new(verbose = FALSE, label = "A reel simple example on iris df")) \%L>\%
  as_tibble()  \%L>\%
  filter(Sepal.Length < 5) \%L>\%
  mutate(rn = row_number()) \%L>\%
  group_by(Species) \%L>\%
  summarise(n = n_distinct(rn)) \%L>\%
  dump_log(file=logfile)

logdata <- read.csv(logfile)

head(logdata)

  
}

}
\seealso{
\code{\link[=logrittr_options]{logrittr_options()}}, \code{\link{\%>=\%}()}, \link{pipe_log}
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{label}}{Set by lumberjack to the name of the tracked object.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-logrittr_logger-new}{\code{logrittr_logger$new()}}
\item \href{#method-logrittr_logger-add}{\code{logrittr_logger$add()}}
\item \href{#method-logrittr_logger-dump}{\code{logrittr_logger$dump()}}
\item \href{#method-logrittr_logger-clone}{\code{logrittr_logger$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-logrittr_logger-new"></a>}}
\if{latex}{\out{\hypertarget{method-logrittr_logger-new}{}}}
\subsection{Method \code{new()}}{
Create a new \code{logrittr_logger}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{logrittr_logger$new(verbose = TRUE, src_name = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{verbose}}{Logical. Whether to print log messages to the console.
Default \code{TRUE}.}

\item{\code{src_name}}{Character. Optional name of the source object,
displayed as a header rule before the first step.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-logrittr_logger-add"></a>}}
\if{latex}{\out{\hypertarget{method-logrittr_logger-add}{}}}
\subsection{Method \code{add()}}{
Called by lumberjack after each pipe step.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{logrittr_logger$add(meta, input, output)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{meta}}{List with elements \code{expr} and \code{src} (the step expression).}

\item{\code{input}}{Data frame before the step.}

\item{\code{output}}{Data frame after the step.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-logrittr_logger-dump"></a>}}
\if{latex}{\out{\hypertarget{method-logrittr_logger-dump}{}}}
\subsection{Method \code{dump()}}{
Called by \code{dump_log()}. Writes accumulated log to a CSV
file. If \code{verbose = TRUE}, also prints the file path.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{logrittr_logger$dump(file = NULL, ...)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{file}}{Character. Output file path. Defaults to \code{"simple.csv"} or
\code{"<label>_simple.csv"} when a label is set.}

\item{\code{...}}{Additional arguments passed to \code{\link[=write.csv]{write.csv()}}.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-logrittr_logger-clone"></a>}}
\if{latex}{\out{\hypertarget{method-logrittr_logger-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{logrittr_logger$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
