% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activate.R
\name{logrittr_hook}
\alias{logrittr_hook}
\title{knitr source hook for pipe logging}
\usage{
logrittr_hook(pipe = c("native", "magrittr", "both"))
}
\arguments{
\item{pipe}{Character. Which pipe(s) to intercept. One of \code{"native"},
\code{"magrittr"}, or \code{"both"}. Default: \code{"native"}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Registers a knitr source hook that enables logrittr logging in R Markdown
and Quarto documents, with no change to pipeline code.
\itemize{
\item \code{"native"} (default): rewrites \verb{|>} to \verb{\%>=\%} in chunks where the chunk
option \code{logrittr = TRUE} is set.
\item \code{"magrittr"}: calls \code{\link[=logrittr_activate]{logrittr_activate()}} so \verb{\%>\%} logs globally.
\item \code{"both"}: does both of the above.
}
}
\examples{
\dontrun{
# In your setup chunk:
library(logrittr)

knitr::opts_chunk$set(
collapse  = TRUE,
comment   = "#>",
message   = TRUE   # needed to show logrittr output (uses message())
)

# For |> pipes (opt-in per chunk with logrittr = TRUE):
logrittr_hook()

# For \%>\% pipes (global):
logrittr_hook("magrittr")

# For both:
logrittr_hook("both")

# Then in any chunk you want logged (native pipe):
# ```{r, logrittr = TRUE}
# iris |>
#   filter(Sepal.Length < 5) |>
#   group_by(Species) |>
#   summarise(n = n())
# ```
}

}
\seealso{
\code{\link[=logrittr_activate]{logrittr_activate()}}, \code{\link[=logrittr_deactivate]{logrittr_deactivate()}}
}
