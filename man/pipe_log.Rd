% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe.R
\name{\%>=\%}
\alias{\%>=\%}
\alias{pipe_log}
\title{Logging pipe operator}
\usage{
lhs \%>=\% rhs
}
\arguments{
\item{lhs}{A data frame (or tibble) passed as the left-hand side.}

\item{rhs}{An unevaluated dplyr-style function call.}
}
\value{
The result of applying \code{rhs} to \code{lhs}, invisibly from the logging
perspective — the value is returned normally so pipelines compose as
expected.
}
\description{
A drop-in replacement for \verb{\%>\%} that logs row counts, column counts,
added/dropped column names, and step timing at each stage of a dplyr / tidyr pipeline.
Inspired by the SAS DATA step log and mostly for educational context in R.

Nested pipelines (e.g. inside a \code{semi_join()} or \code{filter()} argument) are
automatically detected and displayed with increasing indentation, so the
main pipeline and its sub-pipelines are visually distinct.
}
\details{
Depth tracking uses \code{options(.LPipe_depth)} which is incremented around the
evaluation of \code{rhs} only, ensuring that the steps of the \emph{main} pipeline
always log at depth 0 and nested \verb{\%>=\%} calls at depth 1, 2, etc.

Display options (\code{wrap_width}, \code{big_mark}, \code{lang}) are controlled via
\code{\link[=logrittr_options]{logrittr_options()}}.
}
\examples{
\dontrun{
library(dplyr)

logrittr_options(lang = "en", big_mark = ",", wrap_width = 30)

iris \%>=\%
  as_tibble() \%>=\%
  filter(Sepal.Length < 5) \%>=\%
  mutate(rn = row_number()) \%>=\%
  group_by(Species) \%>=\%
  summarise(n = n_distinct(rn))
}


}
\seealso{
\code{\link[=logrittr_options]{logrittr_options()}}
}
